.row.justify-content-center
  .col-md-6
    .card
      .card-header
        %h3
          .fa.fa-shopping-bag
          New product
      .card-body
        - if current_user.info_complete?
          = render 'form'
        - else
          Hi
      .card-footer
        = link_to 'Back', products_path

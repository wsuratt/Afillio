- content_for :title do
  How it works
  \-
  Support
  \-
  Afillio

#wrapper.d-md-flex

  = render 'support/sidebar'
  
  #page-content-wrapper
    .container-fluid
      %h1.mt-4 How it works:
      %p
      %h4
        For Sellers:
      .card-group
        .card.card-body
          .p-3
            %h1
              .fa.fa-shopping-bag
            Find a product to sell
        .card.card-body
          .p-3
            %h1
              .fa.fa-qrcode
            Share your link with a customer or have them scan your QR code
        .card.card-body
          .p-3
            %h1
              .fa.fa-check-square
            Customer completes purchase
        .card.card-body
          .p-3
            %h1
              .fa.fa-hand-holding-usd
            You get paid
      - if current_user
        = link_to 'Get started as a Seller', products_path
      - else
        = link_to 'Get started as a Seller', new_user_registration_path
      %p
      %h4
        For Vendors:
      .card-group
        .card.card-body
          .p-3
            %h1
              .fa.fa-user-plus
            Sign up (your account will be set to Seller by default)
        .card.card-body
          .p-3
            %h1
              .fa.fa-align-justify
            Submit Vendor form (click become vendor link below) and wait to be approved
        .card.card-body
          .p-3
            %h1
              .fa.fa-shopping-bag
            List your products on the site
        .card.card-body
          .p-3
            %h1
              .fa.fa-share-square
            Share your products to get people interested in selling them
      - if current_user
        = link_to 'Get started as a Vendor', vendor_info_user_path(current_user)
      - else
        = link_to 'Get started as a Vendor', become_vendor_support_index_path